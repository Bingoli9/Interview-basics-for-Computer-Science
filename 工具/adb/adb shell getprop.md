## adb shell getprop

get property via the android property service

getprop \[options]

```C++
getprop
getprop ro.build.version.sdk
getprop ro.chipname
getprop | grep adb
```

## adb sehll setprop

set property service

```C++
setprop <key> <value>
```

