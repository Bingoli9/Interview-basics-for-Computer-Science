  #关于程序的编译和链接
  程序需要经过编译和链接过程，在unix编译生成.o文件，即Object File。然后把大量Object File合成执行文件，这个动作叫链接。
  在大多数时候，由于源文件太多，编译生成中间目标文件太多，需要给中间目标文件打个包，在unix下是archive file，也就是.a文件。
  make的过程为：
  源文件首先会生成中间目标文件，在由中间目标文件生成执行文件。在编译时，编译器会检测程序语法，和函数，变量是否被声明。如果函数未被声明，编译器发出       
  警告，但仍会生成o文件。在链接过程，会寻找函数实现，上述未被声明但函数会引起链接错误。
  
